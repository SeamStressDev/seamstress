{
  "must_find": [
    {
      "id": "idempotency-coverage-asymmetry",
      "description": "Idempotency protects the legacy charge path but not the payment-intents path; a retry through the unprotected path is treated as a fresh charge and double-charges. The bug is coverage asymmetry between two money-moving paths, not the absence of idempotency.",
      "match": {
        "seam_kind": "money_path",
        "blast_radius_min": "high",
        "all_of": [
          ["idempoten", "retry.?key", "dedup"],
          ["one .{0,15}path", "only .{0,15}(the )?(legacy|charges)", "not .{0,15}(the )?(intent|payment.?intent)", "second .{0,10}path", "asymmetr", "missing on", "one of (the )?two", "not both", "(passes|attaches|has|includes|generates?|sets?|adds?|uses?|provides?|dedup) .{0,30}(idempoten|key).{0,220}(no|without|missing|lacks?|omit|skip|does ?n'?t|do ?n'?t|fail|never) .{0,30}(idempoten|key|opts)", "(no|without|missing|lacks?|omit|skip|does ?n'?t|do ?n'?t|fail|never) .{0,30}(idempoten|key|opts).{0,220}(passes|attaches|has|includes|generates?|sets?|adds?|uses?|provides?|dedup) .{0,30}(idempoten|key)"],
          ["retry", "retried", "re-?fire", "duplicate", "double.?charg", "charged .{0,10}(twice|again|multiple)"]
        ]
      }
    }
  ],
  "must_not_claim": [
    {
      "id": "protected-path-is-defective",
      "description": "The legacy path is correctly protected. A review must not claim the idempotency implementation on the legacy/charges path is the defect.",
      "match": {
        "asserted_claim": {
          "subject": ["legacy", "chargevialegacy", "charges path", "first path"],
          "predicate": ["broken", "defect", "defective", "wrong", "incorrect", "bug", "flaw", "fails", "fail"],
          "within": 50
        }
      }
    },
    {
      "id": "idempotency-missing-entirely",
      "description": "Idempotency is not absent everywhere — it exists on one path. A review claiming there is no idempotency at all has misread the coverage-asymmetry mechanism.",
      "match": {
        "asserted_claim": {
          "subject": ["idempotency", "idempotent", "idempoten", "dedup", "deduplication"],
          "predicate": ["no", "without", "missing", "absent", "lacks", "lack", "none", "never"],
          "scope": ["anywhere", "at all", "entirely", "throughout", "whatsoever", "everywhere", "in the code", "in this code", "across the code"],
          "within": 60
        }
      }
    }
  ]
}
